# Workaround: libgit2-sys 0.14.x (transitive dep via built → git2) doesn't emit
# cargo:rustc-link-lib=advapi32 on Windows, causing the rust_road_router build
# script to fail with 15 unresolved externals from advapi32.
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "link-arg=advapi32.lib"]
